--TEST--
"mapping" test
--TEMPLATE--
{{ empty is mapping ? 'ok' : 'ko' }}
{{ sequence is mapping ? 'ok' : 'ko' }}
{{ traversable is mapping ? 'ok' : 'ko' }}
{{ obj is mapping ? 'ok' : 'ko' }}
{{ mapping is mapping ? 'ok' : 'ko' }}
{{ string is mapping ? 'ok' : 'ko' }}
--DATA--
return [
    'empty' => [],
    'sequence' => [
        'foo',
        'bar',
        'baz'
    ],
    'traversable' => new \ArrayIterator([]),
    'obj' => new \stdClass(),
    'mapping' => [
        'foo' => 'bar',
        'bar' => 'foo'
    ],
    'string' => 'test',
]
--EXPECT--
ok
ko
ko
ko
ok
ko
