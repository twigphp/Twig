--TEST--
"is_array" test
--TEMPLATE--
{{ foo is array ? 'ok' : 'ko' }}
{{ obj is array ? 'ok' : 'ko' }}
{{ val is array ? 'ok' : 'ko' }}
--DATA--
return array(
    'foo' => array(),
    'obj' => new stdClass(),
    'val' => 'test',
);
--EXPECT--
ok
ko
ko