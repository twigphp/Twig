``array``
=========

``array`` checks if a variable is an array:

.. code-block:: jinja

    {# evaluates to true if the foo variable is an array #}
    {% if foo is array %}
        ...
    {% endif %}
