``capitalize first``
==============

The ``capitalize_first`` filter capitalizes a first character of a string.

.. code-block:: jinja

    {{ 'i love HTML'|capitalize_first }}

    {# outputs 'I love HTML' #}
